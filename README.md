# ⚡ systemctl start shashiazad.service

I build core backend infrastructure, distributed cloud automation tools, and multi-agent AI workflows. I lean heavily into type safety, strict API contracts, and structural automation so humans don't have to handle repetitive configurations.

---

## 🛠️ System Spec Sheet

```mermaid
graph LR
    subgraph System & Infrastructure
    Go --> K8s[Kubernetes]
    C++ --> Linux[POSIX / Core]
    C --> Memory[Low-level Memory]
    end
    subgraph AI Orchestration
    Python --> LangGraph[Agentic Graphs]
    LangGraph --> Gemini[Gemini API]
    end
